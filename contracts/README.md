# Contracts

This directory stores TTC integration contracts.

- `openapi/`  : TTC HTTP APIs (BFF, admin, etc.)
- `mcp/`      : MCP tool contracts (tool names, input/output schemas)
- `schemas/`  : Schema copies when contracts are published as a bundle

The reference deployment is AWS-first. Contracts remain vendor-neutral.
