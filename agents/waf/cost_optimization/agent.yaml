id: waf.cost_optimization
name: WAF Cost Optimization
version: 0.1.0
description: WAF analysis agent for cost optimization. Produces findings with evidence references.
inputs:
  - snapshot_id
outputs:
  - findings
contracts:
  finding_schema: schemas/finding.schema.json
